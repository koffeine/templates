spring:
    web:
        resources:
            add-mappings: false
    mvc:
        throw-exception-if-no-handler-found: true

logging:
    level:
        org.springframework.web.servlet.PageNotFound: off
